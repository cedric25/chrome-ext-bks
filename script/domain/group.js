'use strict';

/**
 * Classe 'Group'
 * @param id
 * @param title
 * @param parentId
 */
var Group = function(id, title, parentId) {
  this.id = id;
  this.title = title;
  this.parentId = parentId;
};